import React from 'react';
import ReactDOM from 'react-dom';
import {Card, Button, Row, Col, Container} from 'react-bootstrap';

export class MyVocabContainer extends React.Component {

    render () {
	return (
	    <div>
	    <p>You don't know any words tbh </p>
            </div>
        );
    }
}
